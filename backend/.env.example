# Copy this file to `.env` and update values before running the app
# Example (.env)

# PostgreSQL connection string
# Format: postgresql://<user>:<password>@<host>:<port>/<db_name>
DATABASE_URL=postgresql://postgres:postgres123@localhost:5432/mentorship

# Secret key for JWT signing (override for production)
SECRET_KEY=replace_this_with_a_strong_secret
